# 2bttns

## Setup

### First-time setup

````bash

```bash
$ npm install           # Install npm dependencies
$ npm run db-sync:dev   # Prisma db setup
````

### Run the app

```bash
$ npm run dev           # Start the app
```
